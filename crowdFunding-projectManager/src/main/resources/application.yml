server:
  port: 5000
spring:
  application:
    name: project-manager
  cloud:
    loadbalancer:
      ribbon:
        enabled: false
eureka:
  client:
    service-url:
      defaultZone: http://45.32.64.240:8761/eureka/
  instance:
    prefer-ip-address: true